# MetaPip Web - Environment Variables (example)
# Copy this file to .env.local and fill in your values.

# --- Supabase (required) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# If you use Supabase Edge Functions (optional):
NEXT_PUBLIC_SUPABASE_FUNCTIONS_URL=

# --- Clerk (web) (optional but recommended for web auth) ---
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# Optional additional Clerk values if you use hosted OAuth flow
NEXT_PUBLIC_CLERK_CLIENT_ID=
NEXT_PUBLIC_CLERK_OAUTH_CLIENT_ID=
NEXT_PUBLIC_CLERK_BASE_URL=
NEXT_PUBLIC_CLERK_OAUTH_BASE=
NEXT_PUBLIC_CLERK_HOSTED_URL=
# Template name for Clerk JWT used to activate Supabase session (defaults to "supabase")
NEXT_PUBLIC_CLERK_SUPABASE_TEMPLATE=

# --- App Flags ---
# If set to 1/true/yes, runs in desktop-only mode (Clerk disabled on web UI)
NEXT_PUBLIC_DESKTOP_DISABLE_CLERK=0
# Enables client-side Supabase session bridging via Clerk
NEXT_PUBLIC_SUPABASE_SESSION_ENABLED=1
# Choose identity provider for signInWithIdToken: "clerk" (default) or "oidc"
NEXT_PUBLIC_SUPABASE_IDENTITY_PROVIDER=clerk
# Session mode for Clerk→Supabase bridge: "id_token" (default, recommended) or "set_session"
# - id_token: exchanges Clerk/OIDC token for a Supabase session (supports refresh)
# - set_session: directly sets Supabase session with Clerk JWT (no refresh)
NEXT_PUBLIC_SUPABASE_SESSION_MODE=id_token

# --- Dev helpers (optional) ---
NEXT_PUBLIC_TOKEN_FILE=
NEXT_PUBLIC_DISABLE_TOKEN_FILE=
NEXT_PUBLIC_TOKEN_DIR=
