export default function App() {
  // Legacy Vite shell kept only to satisfy TypeScript build; not used by Next.js app.
  return (
    <main className="p-6 max-w-3xl mx-auto">
      <h1 className="text-2xl font-semibold">MetaPip (Legacy Shell)</h1>
      <p className="mt-2 text-sm text-zinc-600 dark:text-zinc-300">
        این فایل دیگر استفاده نمی‌شود و فقط برای سازگاری با بیلد باقی مانده است.
      </p>
    </main>
  );
}
