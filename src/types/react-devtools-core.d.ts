declare module "react-devtools-core" {
  // Minimal type for connectToDevTools used in development only
  export function connectToDevTools(options?: { host?: string; port?: number }): void;
  const _default: any;
  export default _default;
}
